Cacheable interface / abstract class that handles the __set_state for you?


Forms:


Database:
    - SQL as methods in classes, kind of like components
    - Oh shit we could actually just use the same tech for SQL files as components

Static files (css, js, etc.):